@model cwagnerPortfolio.Models.Blog

@{
    ViewBag.Title = Model.Title;
}

<div id="blogpost" class="inner-content">

    <section id="page-title" class="inner-section"
             data-animation-origin="right"
             data-animation-duration="500"
             data-animation-delay="300"
             data-animation-distance="25px">
        <div class="container-fluid nopadding">
            @Html.Partial("_LoginPartial")
            <h2 class="font-accident-two-normal uppercase">@Html.DisplayFor(model => model.Title)</h2>
            <div class="post-meta"><span>by <a href="#!">Christina</a>,</span> <span>@Html.DisplayFor(model => model.Created.Date)</span></div>
        </div>
    </section>

    <!-- Blog Block -->
    <section class="inner-section">

        <div class="container-fluid nopadding">

            <img src="@Html.DisplayFor(model => model.MediaUrl)" class="img-responsive"
                 data-animation-origin="top"
                 data-animation-duration="500"
                 data-animation-delay="300"
                 data-animation-distance="25px">

            <article class="post"
                     data-animation-origin="top"
                     data-animation-duration="500"
                     data-animation-delay="300"
                     data-animation-distance="25px">

                <div class="dividewhite6"></div>

                <!-- Post Content -->
                @Html.Raw(Model.Body)
                <!-- /Post Content -->

                <div class="dividewhite4"></div>
                <hr>

            </article>

        </div>
        <div class="dividewhite8"></div>

        <p>
            @if (User.IsInRole("Admin"))
            {
                @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) <span> | </span>;
                @Html.ActionLink("Delete", "Delete", new { id = Model.Id }) <span> | </span>;
                @Html.ActionLink("Back to List", "Index");
            }
            else if (Request.IsAuthenticated)
            {
                @Html.ActionLink("Back to List", "Index");
            }
            else
            {
                @Html.ActionLink("Back to List", "Index");
            }
        </p>
    </section>
    <!-- /Blog Block -->

</div>
